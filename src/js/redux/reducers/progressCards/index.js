// import * as CONSTANTS from '../../constants'

const progressCards = ( state = [], action ) => {
    switch (action.type) {
        default:
            return state
    }
}

export default progressCards