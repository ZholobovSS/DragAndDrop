// import * as CONSTANTS from '../../constants'

const newCards = ( state = [], action ) => {
    console.log(state)
    switch (action.type) {
        default:
            return state
    }
}

export default newCards